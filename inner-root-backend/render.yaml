services:
  - type: web
    name: inner-root-backend
    runtime: java
    plan: free
    buildCommand: ./mvnw clean install -DskipTests
    startCommand: java -jar target/*.jar
    envVars:
      - key: PORT
        value: 8080
      - fromGroup: inner-root-db-creds
